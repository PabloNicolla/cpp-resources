#pragma once

#include <iostream>
#include <iomanip>
#include <string>
#include <vector>

void displayMenu(std::vector<std::string> menuItems);
void displayIDs(std::vector<std::string> IDs);
void formatCurrency(double amount);
void outputSplit(char c);
